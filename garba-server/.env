# Garba Server Configuration
# Edit these values as needed

# Server Port (default: 3000)
PORT=3000

# Server Host (0.0.0.0 allows access from other devices)
HOST=0.0.0.0

# Log Level (error, warn, info, debug)
LOG_LEVEL=info

# Data Directory (where logs and backups are stored)
DATA_DIR=./data

# Maximum requests per minute per IP
MAX_REQUESTS_PER_MINUTE=1000

# Auto-backup interval in milliseconds (300000 = 5 minutes)
BACKUP_INTERVAL=300000

# Device timeout in milliseconds (300000 = 5 minutes)
DEVICE_TIMEOUT=300000

# Receiver timeout in milliseconds (60000 = 1 minute)  
RECEIVER_TIMEOUT=60000

# Production mode (true/false)
NODE_ENV=development